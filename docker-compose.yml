services:
  mervyn:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - RUST_LOG=mervyn=debug,tower_http=info
