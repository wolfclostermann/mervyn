folder_name  = "service-projects"
project_name = "mervyn-prod"

labels = {
  environment = "prod"
  stack       = "mervyn"
  managed-by  = "terraform"
}

# Optional: override root os_login default (see terraform/variables.tf).
